// vibedit.config.js — optional, all values shown are defaults
module.exports = {
  port: 4242,
  shortcut: 'ctrl+shift+e',
  position: 'bottom-right',
  cssMode: 'inline', // 'inline' | 'module' (tailwind in v1.1)
  panelWidth: 320,
  undoLimit: 50,
  backupDir: '.vibedit-backup',
  prettier: true,
  telemetry: false,
};
